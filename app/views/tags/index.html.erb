<h1>All Tags</h1>

<ul>
  <% @tags.each do |tag| %>
    <li class=<%= "tag_#{tag.id}" %>>
      <%= link_to tag.title, tag_path(tag) %>
      <%= link_to "Edit", edit_tag_path(tag) %>
      <%= link_to "Delete", tag_path(tag), method: "delete" %>
    </li>
  <% end %>
</ul>
